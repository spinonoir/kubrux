#compdef kubrux

# AUTOMATICALLY GENERATED by `shtab`


_shtab_kubrux_commands() {
  local _commands=(
    
  )
  _describe 'kubrux commands' _commands
}

_shtab_kubrux_options=(
  "(- : *)"{-h,--help}"[show this help message and exit]"
  "--host[Default SSH host]:host:"
  {--dir,-d}"[Default working directory]:working_dir:"
  {--script,-s}"[Default script recording filename]:script_file:"
  "--session[tmux session name (default\: kubrux)]:session:"
  "--local[Run locally, no SSH]"
  "--help-scene[Show detailed scene file format]"
  "(- : *)--version[show program\'s version number and exit]"
  {--verbose,-v}"[Enable verbose logging]"
  {--attach,-a}"[Attach this terminal to the tmux session to watch the scene live]"
  "(- : *)--print-completion[print shell completion script]:print_completion:(bash zsh tcsh)"
  ":Scene file (\*.scene):"
)

# guard to ensure default positional specs are added only once per session
_shtab_kubrux_defaults_added=0


_shtab_kubrux() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*' default='*::: :->kubrux'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_kubrux_defaults_added )); then
    if (( ${_shtab_kubrux_options[(I)${(q)one_or_more}*]} +          ${_shtab_kubrux_options[(I)${(q)remainder}*]} +          ${_shtab_kubrux_options[(I)${(q)default}]} == 0 )); then
      _shtab_kubrux_options+=(': :_shtab_kubrux_commands' '*::: :->kubrux')
    fi
    _shtab_kubrux_defaults_added=1
  fi
  _arguments -C -s $_shtab_kubrux_options

  case $state in
    kubrux)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_kubrux-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_kubrux -N kubrux
else
  # autoload from fpath, call function directly
  _shtab_kubrux "$@"
fi

